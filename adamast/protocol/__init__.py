"""The AdaMAST checkpoint protocol.

One implementation of the compact four-field checkpoint (parser, citation
matching, repair heuristic) shared by every host transport, plus the
pre-submission gate and the checkpoint/repair prompt bodies.
"""
